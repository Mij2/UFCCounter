<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ufc_button">
        <item name="android:textSize">@dimen/button_score_size</item>
    </style>

    <style name="ufc_score">
        <item name="android:layout_width">@dimen/score_width</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_alignParentTop">true</item>
        <item name="android:layout_alignParentEnd">true</item>
        <item name="android:textSize">@dimen/scores_size</item>
        <item name="android:text">0</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/score_shape</item>
        <item name="android:layout_margin">6dp</item>

    </style>
</resources>
